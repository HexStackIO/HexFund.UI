<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true">

        <!-- MSAL redirect is handled by MsalActivity.cs via [IntentFilter] attribute.
             Declaring it here as well would create a duplicate registration in the
             merged manifest, causing Android to show an app-chooser dialog on redirect. -->

    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
